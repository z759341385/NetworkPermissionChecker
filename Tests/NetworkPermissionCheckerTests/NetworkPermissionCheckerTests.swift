import XCTest
@testable import NetworkPermissionChecker

final class NetworkPermissionCheckerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NetworkPermissionChecker().text, "Hello, World!")
    }
}
